# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.
#
# See https://github.com/r-lib/actions/tree/master/examples#readme for
# additional example workflows available for the R community.

name: R

on:
  push:
    branches: [ "estable" ]
  pull_request:
    branches: [ "estable" ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]
        r-version: ['4.5.1', 'release', 'oldrel-1']
        # macOS (runners arm64) no siempre tiene binario para el parche exacto
        # '4.5.1' (404 al descargarlo); ahí probamos solo con los alias
        # 'release'/'oldrel-1', que la acción resuelve a una versión disponible.
        exclude:
          - os: macos-latest
            r-version: '4.5.1'

    steps:
      - uses: actions/checkout@v4
      - name: Set up R ${{ matrix.r-version }}
        uses: r-lib/actions/setup-r@v2
        with:
          r-version: ${{ matrix.r-version }}
      # Reemplaza el install.packages()/remotes::install_deps() manual: además de
      # las dependencias de R, esta acción instala los requisitos de sistema por SO
      # (GDAL/GEOS/PROJ para sf, unixODBC para odbc, JDK para rJava/RJDBC, etc.),
      # necesarios para que la instalación funcione en ubuntu-latest/macos-latest
      # y no solo en windows-latest.
      - name: Install dependencies
        uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: any::rcmdcheck
      - name: Check
        run: rcmdcheck::rcmdcheck(args = "--no-manual", error_on = "error")
        shell: Rscript {0}
